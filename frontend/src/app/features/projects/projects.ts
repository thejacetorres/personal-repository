import { Component } from '@angular/core';

@Component({
  selector: 'app-projects',
  standalone: true,
  imports: [],
  templateUrl: './projects.html'
})
export class ProjectsComponent {
  // Aquí podríamos crear un array de imágenes más adelante para no repetir HTML,
  // pero por ahora lo haremos directamente en la plantilla para que veas la estructura visual.
}